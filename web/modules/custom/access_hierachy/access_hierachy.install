<?php

/**
 * @file
 * Install file for the Access Hierarchy module.
 */

/**
 * Implements hook_install().
 */
function access_hierachy_install() {
  // In Drupal 11, module weights are managed through dependencies
  // and the module system automatically handles loading order.
  // No need to manually set weights in the database.
}
